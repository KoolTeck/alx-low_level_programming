#include "main.h"

/**
 * _strlen - returns the length of a string.
 *
 * @s: pointer to chars to check
 *
 * Return: int always true.
 */

int _strlen(char *s)
{
int len;
for (int i = 0; i < s[i]; i++)
{
len = i;
}

return (len + 1);
}
